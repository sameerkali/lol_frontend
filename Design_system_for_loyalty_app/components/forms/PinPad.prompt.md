One-line: The staff PIN pad — shown when the phone is handed across the counter, so keys are 76×64 and the copy addresses staff, not the customer.

```jsx
<PinPad value={pin} onChange={setPin} onComplete={confirm} error="That PIN didn't match. Try again." />
```
