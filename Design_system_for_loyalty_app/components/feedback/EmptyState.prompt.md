One-line: Empty list or table — always tell the owner the next physical action.

```jsx
<EmptyState icon="users" title="No customers yet"
  body="Put the NFC tag on the counter and ask your next guest to tap it."
  action={<Button icon="qr-code">Download the poster</Button>} />
```
