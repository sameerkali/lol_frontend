One-line: The reward unlock moment — the loudest thing in the system; use it only when a milestone actually unlocks.

```jsx
<Celebration title="Free coffee unlocked!" subtitle="Show this to the counter." onDismiss={close} />
```

Confetti is coloured shapes, never emoji. Honour `prefers-reduced-motion` by dropping the transforms.
