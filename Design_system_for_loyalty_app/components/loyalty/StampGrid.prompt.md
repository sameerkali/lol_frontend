One-line: The customer's card — the hero of the customer page; pass `animateFrom` right after a visit is marked so the new stamp lands.

```jsx
<StampGrid total={10} filled={7} milestones={[5, 10]} glyph="coffee" animateFrom={6} />
```
