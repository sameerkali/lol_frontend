One-line: Circular icon-only action for toolbars and panel headers — never for the primary action on a customer screen.

```jsx
<IconButton icon="download" label="Download CSV" variant="secondary" />
```
