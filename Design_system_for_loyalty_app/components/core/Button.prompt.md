One-line: The sticker button — use for every action; `primary` grape for the one main action on a screen, `reward` sun only for Redeem.

```jsx
<Button variant="primary" size="lg" icon="hand" fullWidth>Mark my visit</Button>
```

Variants: primary · reward · success · danger · secondary · ghost. Sizes sm/md/lg (48px min tap target at md).
`wobble` adds the idle attention loop — one per screen, never more.
