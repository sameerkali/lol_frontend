One-line: Lucide icon wrapper — use for every glyph in the system; never emoji, never a unicode character.

```jsx
<Icon name="coffee" size={20} />
```

Requires the Lucide CDN script on the page. Stroke is 2.25 to sit next to the 3px ink borders.
