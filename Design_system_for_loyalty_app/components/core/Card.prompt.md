One-line: Every boxed region in LOL — use `tone="sunk"` for nested areas so shadows never stack.

```jsx
<Card tone="sun" tilt={-1.5}>
  <h3>Free coffee</h3>
</Card>
```

Tilt is a garnish: at most one tilted card in view, never more than ±3deg.
